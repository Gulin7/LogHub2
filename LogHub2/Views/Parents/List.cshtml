@model List<LogHub2.Models.Entities.Parent>

@if (true)
{
    <h1>Parents</h1>

    <div class="form-group">
        <div class="form-group">
            <label class="form-label">Sort by Type</label>
            <select class="form-select" id="type-sort">
                <option value="">All Types</option>
                <option value="System">System</option>
                <option value="Security">Security</option>
                <option value="Audit">Audit</option>
            </select>
        </div>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Type</th>
            </tr>
        </thead>
        <tbody id="logs-tbody">
            @foreach (var parent in Model)
            {
                <tr class="parent-row"  data-type="@parent.Type">
                    <td>@parent.Id</td>
                    <td>@parent.Type</td>
                    <td>@parent.Name</td>
                </tr>
            }
        </tbody>
    </table>
}

<script>

    document.getElementById('type-sort').addEventListener('change', function () {
            filterLogs();
        });

        function filterLogs() {
              var selectedType = document.getElementById('type-sort').value;

              var rows = document.querySelectorAll('.parent-row');
           rows.forEach(function(row) {
                var rowType = row.getAttribute('data-type');

                var matchesType = !selectedType || rowType === selectedType;

                if (matchesSeverity && matchesType) {
                    row.style.display = '';
                } else {
                    row.style.display = 'none';
                }
            });
        }
</script>
